
const state = {
  loading: false
}

const getters = {
  
}

const actions = {
  
}

const mutations = {
  setLoading(state) {
    state.loading = true
  }
}

export default {
  state,
  getters,
  actions,
  mutations
}